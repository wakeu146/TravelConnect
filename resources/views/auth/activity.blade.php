<x-account-shell title="{{ __('messages.my_activity') }} | TravelConnect">
    <main class="min-h-screen bg-[#f7f8f6] px-5 py-8 text-[#173042] sm:px-8 lg:px-10 lg:py-10">
        <div class="mx-auto max-w-5xl"><div class="border-b border-[#e4e9e7] pb-7"><p class="text-xs font-bold uppercase tracking-[.2em] text-[#e76f51]">{{ __('messages.my_activity') }}</p><h1 class="mt-2 font-serif text-4xl tracking-tight">{{ __('messages.activity_title') }}</h1><p class="mt-3 text-sm text-[#607985]">{{ __('messages.activity_intro') }}</p></div><div class="mt-8 grid gap-8 lg:grid-cols-2"><section><h2 class="font-serif text-2xl">{{ __('messages.open_inquiries') }}</h2><div class="mt-4 grid gap-3">@forelse ($inquiries as $inquiry)<article class="dashboard-panel border-[#e7eceb] bg-white p-5"><div class="flex items-center justify-between gap-3"><h3 class="font-bold">{{ $inquiry->subject }}</h3><span class="rounded-full bg-[#edf4f3] px-2.5 py-1 text-xs font-bold text-[#2f9e7a]">{{ ucfirst($inquiry->status->value) }}</span></div><p class="mt-2 text-sm text-[#607985]">{{ $inquiry->agency->company_name }}</p></article>@empty<div class="dashboard-panel border-dashed border-[#cbd9d8] bg-transparent p-6 text-sm text-[#607985]">{{ __('messages.no_inquiries_yet') }}</div>@endforelse</div></section><section><h2 class="font-serif text-2xl">{{ __('messages.reviews_written') }}</h2><div class="mt-4 grid gap-3">@forelse ($reviews as $review)<article class="dashboard-panel border-[#e7eceb] bg-white p-5"><div class="flex items-center justify-between gap-3"><h3 class="font-bold">{{ $review->agency->company_name }}</h3><span class="text-[#e76f51]">{{ $review->rating }} ★</span></div><p class="mt-2 text-sm leading-6 text-[#607985]">{{ $review->comment }}</p></article>@empty<div class="dashboard-panel border-dashed border-[#cbd9d8] bg-transparent p-6 text-sm text-[#607985]">{{ __('messages.no_reviews_yet') }}</div>@endforelse</div></section></div></div>
    </main>
</x-account-shell>
